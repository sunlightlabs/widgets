<div id="sideBubble">
  <h3>Politiwidgets</h3>
  <ul id="sideNav">
    <% widgets.each do |slug, widget| %>
      <li>
        <a href="<%= widget_path :id => slug, :legislator => (@legislator ? id_for(@legislator) : settings[:featured][:legislator]) %>"><%= widget[:name] %></a>
      </li>
    <% end %>
  </ul>
</div>